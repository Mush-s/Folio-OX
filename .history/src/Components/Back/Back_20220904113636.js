import './Back.css'

import ReactDOM from 'react-dom'

const Back = () => {
    const histroy=
    const backHandler=()=>{

    }
  return (
    <div className="back">
      <button onClick={backHandler}>⇦</button>
    </div>
  );
};

export default Back;
